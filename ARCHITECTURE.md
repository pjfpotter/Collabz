# Architecture

A plain-English overview of how this system fits together — for us, not for the spec. Detailed, capability-by-capability specs live in `openspec/specs/`; this is the "explain it to a beginner" summary.

Update this as the last step whenever `/opsx:archive-change` lands a change that alters the shape of the system (new component, changed data flow, new dependency, new integration). Skip it if the change was purely internal with no structural effect.

## What this app does

<!-- One or two sentences: the core user problem and what the app does about it -->

## The pieces

<!-- List the main parts once they exist — e.g. frontend, API, database, any external service — one line each on what it's for -->

## How a request flows through the system

<!-- e.g. User action → screen → server → saved data → result. Update this diagram/description as it becomes real -->

## Key decisions and why

<!-- One line per decision: what we chose, and the one-sentence reason. e.g. "SQLite over Postgres — no hosting to manage for a coursework project." -->

## Open questions / known gaps

<!-- Anything not yet decided or not yet built, so it's visible rather than silently assumed -->
